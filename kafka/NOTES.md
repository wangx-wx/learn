# 教学笔记

## 用户偏好
- 中文授课
- 每次约 1 小时
- 要「理念 + 实操」并重；明确要求「牢记」→ 每课必须有检索练习（先回忆再看答案），并做间隔重复
- 已有 Java / Spring 背景，用过 Kafka 客户端 API → **不要**从「什么是消息队列」讲起
- 有可部署 Docker 的服务器；本机 Mac 无 Docker，但有 Java 21

## 授课决策
- 本机无 Docker 且沙箱无网络 → 实操步骤一律设计为在**用户的服务器**上通过 `docker exec` 执行，
  这样可以绕开 `advertised.listeners` 的坑，保证 1 小时内能跑通
- 每课都要有一手资料链接；本轮链接未核验，见 RESOURCES.md 顶部提示
- 术语表采用「掌握状态」标记：只有用户验证过才标 ✅，避免术语表变成一本没读懂的字典
- 参考文档用 HTML（打印友好），课程用 HTML，状态类文档（MISSION / 学习记录）用 Markdown

## 待办
- [ ] 首次联网后核验 RESOURCES.md 所有链接
- [ ] 锁定具体业务场景，替换 MISSION.md 里「订单事件流」这个占位假设
- [ ] 用户答完第 1 课检索练习后，写 learning-records/0001-*.md
